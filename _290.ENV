DATABASE_URL="file:./dev.db"
SESSION_SECRET="dev-secret-change-me"

# Face ID (WebAuthn). Defaults in lib/webauthn.ts work for local dev
# (localhost:3001). Once this app is deployed online, set these two to the
# real domain -- changing them later invalidates every registered device.
# WEBAUTHN_RP_ID="fieldphotos.primecoreps.com"
# WEBAUTHN_ORIGIN="https://fieldphotos.primecoreps.com"
